package entities;

import java.awt.image.BufferedImage;

public class SpecialKey extends Entity {

	public SpecialKey(int x, int y, int width, int height, BufferedImage sprite) {
		super(x, y, width, height, sprite);
	}
}